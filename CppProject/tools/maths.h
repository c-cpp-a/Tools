#ifndef MATHS_H_H
#define MATHS_H_H
#include "BigInt.h"
BigInt Abs(const BigInt x);                  //求绝对值
BigInt Sqrt(const BigInt x);                 //开平方根
UnsignedBigInt Sqrt(const UnsignedBigInt x); //开平方根
#endif